## 2026-05-18 AI 对话多模态 Agent 化文档口径更新

- [x] 恢复现有文档与仓库状态，确认本轮只改总体开发文档和任务锚点。
- [x] 将生图主路径改为 `gpt-image-2` 直出，不再以模板/混合渲染作为 MVP 正式目标。
- [x] 补齐高置信自动异步生成、低置信人工干预/前端确认的流程说明。
- [x] 调整后端、前端、阶段计划、验收标准中的一致性口径。
- [x] 复核文档关键词，确认没有残留冲突口径。

## 2026-05-20 AI 视觉生图失败审计状态不一致

- [x] 恢复 `.tasks` 与视觉链路上下文。
- [x] 定位 `visual_generation` 审计写入点和实际 job 执行失败点。
- [x] 修复失败状态传播或审计语义误标。
- [x] 增加/运行 focused validation，覆盖外部生图 API 断连失败路径。
- [x] 执行后端启动验证；按是否有前端改动决定前端验证范围。
- [x] 更新 `bug.md` 并收口项目负责人视角状态。

## 2026-05-20 AI 视觉生图上游断连稳定性优化

- [x] 恢复 `image_client.py`、AI chat 客户端重试模式和当前审计链路。
- [x] 实现生图 API 网络断连有限重试、连接池重置和 HTTP/2 降级。
- [x] 补充 focused validation：断连后重试成功、重试耗尽失败。
- [x] 更新 `bug.md` / `memory.md` / `.tasks` 真值记录。
- [x] 执行全量测试、后端启动验证、前端启动抽查。

## 2026-05-20 AI 视觉生图连接错误根因诊断脚本

- [x] 恢复现有 `image_client.py` 和配置上下文。
- [x] 新增分层诊断脚本，默认不真实生图、不泄露密钥。
- [x] 运行低成本诊断，给出当前最可能原因。
- [x] 验证脚本语法和必要时补充文档/进度记录。
